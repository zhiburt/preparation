@*
    @using Microsoft.AspNetCore.Identity
            @inject UserManager<IdentityUser> UserManager
        @inject SignInManager<IdentityUser> SignInManager
        @if (SignInManager.IsSignedIn(User))*@


@if (User.Identity.IsAuthenticated)
{
    <li class="nav-item dropdown list-unstyled">
        <a class="bell dropdown-toggle" href="#" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false"><i class="fa fa-2x fa-bell-o text-light"></i></a>
        <div class="dropdown-menu bg-dark">
            <ul class="list-group">
                <li class="list-group-item d-flex justify-content-between align-items-center">
                    Medical
                    <span class="badge badge-primary badge-pill">14</span>
                </li>
                <li class="list-group-item d-flex justify-content-between align-items-center">
                    Updates
                    <span class="badge badge-primary badge-pill">2</span>
                </li>
            </ul>
            <div class="dropdown-divider"></div>
            <div class="dropdown-item">
                <div class="row">
                    <div class="col col-12 text-center">
                        <a class="text-white" asp-controller="AdminMessenger" asp-action="Index"><i class="fa fa-level-up"></i></a>
                    </div>
                    <div class="col col-6 text-center">
                        <form asp-controller="Account" asp-action="LogOff" method="post">
                            <button class="text-white btn btn-link" type="submit"><i class="fa fa-sign-out"></i></button>
                        </form>
                    </div>
                    <div class="col col-6 text-center">
                        <a class="text-white" href="#"><i class="fa fa-support"></i></a>
                    </div>
                    
                </div>
            </div>
        </div>
    </li>
}
else
{

    {
        await Html.RenderPartialAsync("_UserSighIn", new preparation.ViewModels.Account.UserLoginViewModel());
    }
}